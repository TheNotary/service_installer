require 'spec_helper'

describe ServiceInstaller do
  it 'has a version number' do
    expect(ServiceInstaller::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end


end
